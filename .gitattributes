# Normalize line endings: store and check out text as LF on every platform.
# Eliminates the CRLF/LF divergence where local `format:check` (Windows, autocrlf)
# disagrees with CI (Linux, LF).
* text=auto eol=lf

# Binary assets — never normalize.
*.png binary
*.svg binary
